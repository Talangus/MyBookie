class Bet {
    constructor(type, odds) {
        //if validInput(type, odds) continue else null
        //renameForConvention()
        this.odds = odds
        this.type = type
    } 
}

module.exports = Bet;